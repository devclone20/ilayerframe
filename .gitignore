# ── Secrets & credentials — never commit these ───────────────────────────────
.env
.env.*
*.env
*.pem
*.key
*.keystore
*.p12
*.pfx
*.crt
id_rsa*
id_ed25519*
*.secret
secrets.*
credentials.*
*.token
*token*.json
.npmrc
.netrc
# wallets / chain key material
*.wallet
*wallet*.json
keystore/
mnemonic*
seed*.txt
*.privkey

# ── OS / editor cruft ────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo
*~

# ── Build / deps (for the future native apps) ────────────────────────────────
node_modules/
dist/
build/
out/
.cache/
coverage/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# ── Local / scratch ──────────────────────────────────────────────────────────
tmp/
temp/
*.local
scratch/

# ── training (generated in CI, uploaded as artifact) ─────────────────────────
training_reports/
__pycache__/
